install (FILES "lock.h main.h" DESTINATION "${CMAKE_INSTALL_INCLUDEDIR}/${CURRENT_INCLUDEDIR}")
set (SOURCES ${SOURCES}
    "${CMAKE_CURRENT_SOURCE_DIR}/lock.c"
    "${CMAKE_CURRENT_SOURCE_DIR}/main.c"
)

set (SOURCES ${SOURCES} PARENT_SCOPE)
