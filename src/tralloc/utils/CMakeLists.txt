if (${TRALLOC_UTILS_BUFFER})
    install (FILES "buffer.h" DESTINATION "${CMAKE_INSTALL_INCLUDEDIR}/${CURRENT_INCLUDEDIR}")
    set (SOURCES ${SOURCES}
        "${CMAKE_CURRENT_SOURCE_DIR}/buffer.c"
    PARENT_SCOPE)
endif ()
