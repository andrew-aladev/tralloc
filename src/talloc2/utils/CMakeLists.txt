set (LOCAL_INCLUDES)
set (LOCAL_SOURCES)

if (TALLOC_UTILS_BUFFER MATCHES true)
    set (LOCAL_INCLUDES ${LOCAL_INCLUDES} ${CMAKE_CURRENT_SOURCE_DIR}/buffer.h)
    set (LOCAL_SOURCES  ${LOCAL_SOURCES}  ${CMAKE_CURRENT_SOURCE_DIR}/buffer.c)
endif ()
if (TALLOC_UTILS_DYNARR MATCHES true)
    set (LOCAL_INCLUDES ${LOCAL_INCLUDES} ${CMAKE_CURRENT_SOURCE_DIR}/dynarr.h)
    set (LOCAL_SOURCES  ${LOCAL_SOURCES}  ${CMAKE_CURRENT_SOURCE_DIR}/dynarr.c)
endif ()

set (INCLUDES ${INCLUDES} ${LOCAL_INCLUDES} PARENT_SCOPE)
set (SOURCES  ${SOURCES}  ${LOCAL_SOURCES}  PARENT_SCOPE)