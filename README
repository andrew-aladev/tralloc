Talloc2 is lightweight implementation of simplified talloc api. License - LGPL v3.

Goals:
1. Minimize sizeof talloc_chunk.
2. All important functions has O(1) complexity.
2. Configurable extensions.
3. CMake build system. Project is ready to be added as git submodule to existing CMake project.

mkdir build
cd build
cmake ..
make
make test